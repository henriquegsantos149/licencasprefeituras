# This directory contains secret files
# Each secret is stored in its own file named after the secret key
# 
# To set up secrets:
# 1. Copy the .template files to create your secrets
# 2. Edit each file with the actual secret value
# 3. Never commit the actual secret files (only .template files)
#
# Example:
#   cp DATABASE_PASSWORD.template DATABASE_PASSWORD
#   echo "my-actual-password" > DATABASE_PASSWORD
