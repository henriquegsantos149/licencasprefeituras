# Database Configuration (non-sensitive)
# Sensitive values like DATABASE_PASSWORD go in secrets/ directory
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=licencas_prefeituras
DATABASE_USER=postgres
# DATABASE_PASSWORD is in secrets/DATABASE_PASSWORD file

# API Configuration (non-sensitive)
API_V1_PREFIX=/api/v1
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
# SECRET_KEY is in secrets/SECRET_KEY file

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Environment
ENVIRONMENT=development
